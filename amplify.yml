version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build dependencies for static frontend"
    build:
      commands:
        - bash scripts/generate-env.sh
        - echo "Build complete"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
