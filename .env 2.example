# ═══════════════════════════════════════════
# NeuroNum.ai — Environment Variables
# ═══════════════════════════════════════════
# These are only for LOCAL DEVELOPMENT (server/server.js).
# In production, secrets are passed as SAM template parameters
# and injected into Lambda environment variables automatically.
# ═══════════════════════════════════════════

# Claude API (Anthropic) — used by Tutor Alpha
ANTHROPIC_API_KEY=your-anthropic-api-key

# Gemini Pro API (Google) — used by Tutor Beta
GEMINI_API_KEY=your-gemini-api-key

# Local server
PORT=3000
NODE_ENV=development
